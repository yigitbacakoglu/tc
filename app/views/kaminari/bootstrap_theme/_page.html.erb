<% unless page.current? %>
    <%= link_to page, page.current? ? '#' : url, {:class => "page-link", :remote => remote, :rel => page.next? ? 'next' : page.prev? ? 'prev' : nil} %>
<% else %>
    <%= "<span class='current'>#{page}</span>".html_safe %>
<% end %>