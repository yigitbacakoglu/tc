<% if params[:page] %>
$('.comment-box.view-more').first().remove();
$('#comment_container').append('<%= escape_javascript(render :partial => "/admin/overview/index", :formats => :html) %>');
<% end %>