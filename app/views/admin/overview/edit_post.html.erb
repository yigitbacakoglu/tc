<%= form_for @post, :url => admin_update_post_path(:id => @post.id), :html => {:class => 'form-horizontal'} do |f| %>

    <div class="alert alert fade in">
      <i class="icon-warning-sign"></i> <strong> Changing URL will also change the comments path to be shown ! </strong>
    </div>
    <div class="modal-body">
      <div class="control-group">
        <%= f.text_field :url, :class => "input-xxlarge" %>
      </div>
    </div>
    <br>
    <div class="modal-footer">
      <%= link_to t(:close), "#", "data-dismiss" => "modal", :class => "btn", "aria-hidden" => true %>
      <%= link_to_with_icon "icon-trash", t(:delete), admin_delete_post_path(:id => @post.id) , :class => "pull-left btn btn-danger", :confirm => t(:are_you_sure), :method => :delete %>
      <%= button_tag t(:save), :disable_with => t(:processing), :class => "btn btn-primary" %>
    </div>

<% end %>


