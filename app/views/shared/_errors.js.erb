$('.alert').remove();
<% if target && target.errors.any? %>
<% target.errors.to_a.each do |error| %>
$('section[id=main]').prepend('<div class="alert error fade in">' +
        '<button type="button" class="close" data-dismiss="alert">×</button>' +
        '<i class="icon-bullhorn"></i> <strong> <%= error %> </strong>' +
        '</div>')

<% end %>
<% end %>


